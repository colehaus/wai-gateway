resolver: lts-11.9
packages:
- .
- ../serverless-haskell
extra-deps:
  - amazonka-core-1.6.0
  - amazonka-kinesis-1.6.0
  - amazonka-s3-1.6.0
nix:
  packages:
    - zlib.dev
    - zlib.out
ghc-options:
  "wai-gateway": -Wall -Wcompat -Wincomplete-record-updates -Wincomplete-uni-patterns -Wredundant-constraints
